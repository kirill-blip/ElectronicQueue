export type Admin = {
    FirstName: string;
    LastName: string;
    Login: string;
    Password: string;
    TableNumber: number;
}

export type AdminInfo = {
    FirstName: string;
    LastName: string;
    TableNumber: number;
}